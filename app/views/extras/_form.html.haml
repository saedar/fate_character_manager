= form_for @extra do |f|
  - if @extra.errors.any?
    #error_explanation
      %h2= "#{pluralize(@extra.errors.count, "error")} prohibited this extra from being saved:"
      %ul
        - @extra.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :permission
    = f.text_area :permission
  .field
    = f.label :cost
    = f.text_field :cost
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :overcome
    = f.text_area :overcome
  .field
    = f.label :create
    = f.text_area :create
  .field
    = f.label :attack
    = f.text_area :attack
  .field
    = f.label :defend
    = f.text_area :defend
  .actions
    = f.submit 'Save'
